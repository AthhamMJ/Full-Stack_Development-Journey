function find(letter){
    if (letter =='a' || 'e' || 'i' || 'o' || 'u' || 'A' || 'E' || 'I' || 'O' || 'U'  ){
        console.log(letter+" is a vovel Letter");
    } 
    else{
        console.log(letter+" is not a vovel Letter (Consornent)");
    }
}
find("A");